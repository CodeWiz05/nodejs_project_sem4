# Embedding Service Configuration
EMBEDDING_MODEL_NAME=all-MiniLM-L6-v2
# For local development, you might not need to set this explicitly if using the default
# For production or specific model choices, set it here.